import React from 'react';
import RestaurantList from './RestaurantList';

export const Home = () => {
    return (
        <div>
            <h1>Home</h1>
            <RestaurantList/>
        </div>
    );
}